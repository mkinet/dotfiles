- hosts: localhost
  become: true
  pre_tasks:
    - name: update repositories
      apt: update_cache=yes
      changed_when: False

  tasks:
    - include: tasks/packages.yml
    - include: tasks/repository.yml
# install python ?
# install virtualenv ?
# install python dependencies https://docs.ansible.com/ansible/2.5/modules/pip_module.html?highlight=pip
# configure NAS mountpoints
# pull git repo
#
